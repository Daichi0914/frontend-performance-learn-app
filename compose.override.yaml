services:
  frontend:
    environment:
      - WATCHPACK_POLLING=true
    volumes:
      # マウントしてホットリロードを有効にする
      - ./frontend:/app
      # コンテナ内の node_modules と .next を保護（ホスト側で上書きしないようにする）
      - /app/node_modules
      - /app/.next
